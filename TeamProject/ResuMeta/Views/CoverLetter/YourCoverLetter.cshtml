@model ResuMeta.ViewModels.CoverLetterVM;
@{
    ViewData["Title"] = "Cover Letter - View";
}
@section Styles {
    <link rel="stylesheet" href="~/css/ViewCoverLetter.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.quilljs.com/1.0.0/quill.snow.css" />
}

<div class="text-center">
    <div id="validation-area"></div>
    <div class="alert alert-success alert-dismissable fade show" role="alert" id="validation-success" style="display:none;">
    </div>
    <div class="alert alert-danger alert-dismissable fade show" role="alert" id="validation-error" style="display:none;">
    </div>
    <h1 class="display-4 title">Your Cover Letter</h1>
    @if(Model != null)
    {
        <input type="hidden" id="cover-letter-id" value="@Model.CoverLetterId" />
        <input type="hidden" id="cover-letter-content" value="@Model.HtmlContent" />
        <div class="container" id="cover-letter-area" style="display: none;">

        </div>
        <hr />
        <div class="mt-2 mx-2" style="float: right;">
            <button class="btn btn-primary" id="save-cover-letter" style="margin: 0px 0px;">Save Cover Letter</button>
        </div>
        <div class="mt-2 mx-2" style="float: right;">
            <button class="btn btn-primary" id="export-pdf" style="margin: 0px 0px;">Export PDF</button>
        </div>
        <div class="mt-3 mx-2" style="float: left;">
            <h6><strong>Title:</strong> <input type="text" id="cover-letter-title" value="@Model.Title" /></h6>
        </div>
        <div id="editor" style="height: 100vh; width: 816; margin: 0 auto;">

        </div>
    }
    else
    {
        <div class="alert alert-danger" role="alert">
            <strong>Oops!</strong> No cover letter found.
        </div>
    }
</div>

@section Scripts {
    <script type="text/javascript" src="~/js/YourCoverLetter.js"></script>
}